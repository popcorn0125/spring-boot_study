<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="project.SignUpMember.Mapper.PostMapper">
    <insert id="registerPost" parameterType="project.SignUpMember.domain.Post">
        INSERT INTO post ( title, content, created_date )
        VALUES (  #{title}, #{content}, NOW() )
    </insert>

    <update id="upadtePost" parameterType="project."


</mapper>